export function {{ pascalCase name }}({ children, ...props }: PropsWithChildren<{{ pascalCase name }}Props>) {
    return <Chakra{{ pascalCase name }} {...props}>{children}</Chakra{{ pascalCase name }}>;
}
