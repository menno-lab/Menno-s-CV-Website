service: trpc-hello-world
frameworkVersion: '3'

provider:
    name: aws
    runtime: nodejs14.x

functions:
    http-api:
        handler: dist/server.handler
        events:
            - httpApi: '*'

plugins:
    - serverless-offline
